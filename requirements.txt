# PyTorch with CUDA 12.8 - required for RTX 50-series (Blackwell, sm_120).
# cp314 wheels confirmed available on the cu128 index for Python 3.14.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.9.1+cu128
torchvision==0.24.1+cu128

# Scientific / evaluation stack (unpinned: let pip pick cp314-compatible wheels)
numpy
scikit-learn
matplotlib
seaborn

# Computer vision (real-time demo + Haar cascade face detection)
opencv-python

# Config parsing & progress bars
PyYAML
tqdm
